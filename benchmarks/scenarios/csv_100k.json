{
  "name": "csv_100k",
  "source_type": "csv",
  "rows": 100000,
  "match_sql": "SELECT i.*, p.* FROM left_src i JOIN right_src p ON i.invoice_id = p.reference_number AND tolerance_match(i.amount, p.paid_amount, 0.02)",
  "sources": [
    {
      "alias": "left_src",
      "uri": "file://${DATA_DIR}/invoices.csv",
      "format": "csv"
    },
    {
      "alias": "right_src",
      "uri": "file://${DATA_DIR}/payments.csv",
      "format": "csv"
    }
  ],
  "primary_keys": {
    "left_src": ["invoice_id"],
    "right_src": ["payment_id"]
  }
}
