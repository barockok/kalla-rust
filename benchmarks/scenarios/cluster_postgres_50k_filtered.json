{
    "name": "cluster_postgres_50k_filtered",
    "mode": "cluster",
    "source_type": "postgres",
    "rows": 50000,
    "match_sql": "SELECT i.*, p.* FROM left_src i JOIN right_src p ON i.invoice_id = p.reference_number AND tolerance_match(i.amount, p.paid_amount, 0.02)",
    "filters": {
        "left_src": [
            {"column": "amount", "op": "gte", "value": 10000.0}
        ],
        "right_src": [
            {"column": "paid_amount", "op": "gte", "value": 10000.0}
        ]
    }
}
