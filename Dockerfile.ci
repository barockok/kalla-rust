# Thin runtime image — uses pre-built static musl binary from CI
FROM alpine:3.21
RUN apk add --no-cache ca-certificates curl
ARG TARGETARCH
COPY bin/${TARGETARCH}/kallad /usr/local/bin/
EXPOSE 8080 50050 50051 50052
ENTRYPOINT ["kallad"]
